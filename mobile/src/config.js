// Default backend URL used the first time the app launches.
//
// IMPORTANT: on a real phone (Expo Go or the installed APK) you CANNOT use
// "localhost" — that points at the phone itself. Use the LAN IP of the
// computer running the backend. This was auto-detected during setup.
// You can also change it at runtime from the Login screen ("Server settings").
export const DEFAULT_API_URL = 'http://192.168.0.100:4000';
